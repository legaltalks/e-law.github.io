% 문서 설정 부분
\documentclass[15pt]{oblivoir} % 더블스페이싱으로 했을 때 한 면에 21줄 내외가 오도록 글자 크기 설정
\usepackage{fapapersize}
\usefapapersize{210mm,297mm,20mm,*,45mm,30mm} % A4용지 및 여백 설정

\setkomainfont(HCR Batang) % 폰트를 함초롬체로 설정

\renewcommand{\thesection}{\arabic{section}.} % section 형식을 "1. 제목"의 형식으로 설정
\renewcommand{\thesubsection}{\gana{subsection}.} % subsection 형식을 "가. 제목"의 형식으로 설정

\setsecheadstyle{\normalsize} % section 글자 크기를 본문과 동일하게 설정
\setsubsecheadstyle{\normalsize} % subsection 글자 크기를 본문과 동일하게 설정

\linespread{1.6} % 행간을 더블스페이싱으로 설정

% sectioning division에서 둘째 줄 들여쓰기 되지 않도록 설정
\makeatletter
\def\@hangfrom#1{\setbox\@tempboxa\hbox{{#1}}
  \hangindent 0pt
  \noindent\box\@tempboxa}
\makeatother


% 문서 시작 부분
\begin{document}
\title{소장}
\maketitle
\begin{minipage}{\textwidth}
원고
  \end{minipage}
\end{document}
